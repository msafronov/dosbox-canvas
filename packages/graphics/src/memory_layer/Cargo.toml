[package]
name = "memory_layer"
version = "0.0.0"
edition = "2021"
authors = ["Matvey Safronov"]

[lib]
name = "memory_layer"
path = "src/lib.rs"
test = true
doctest = false

[dependencies]
infrastructure_layer = {path = "../infrastructure_layer"}
errors = {path = "../errors"}

[dev-dependencies]
wasm-bindgen-test = "0.3.0"

[features]
unit_test = []